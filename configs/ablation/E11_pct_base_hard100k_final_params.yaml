# E11: PCT base en Hard-100k con parametros finales comparables.
# Objetivo: comparar el encoder base contra MHA usando M=32, hidden_dim=256,
# w_rsd=0.1 y el mismo protocolo de entrenamiento/evaluacion.
